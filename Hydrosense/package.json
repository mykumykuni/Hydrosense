{
  "name": "hydrosense",
  "version": "1.0.0",
  "description": "Water management system with React frontend and Laravel backend",
  "scripts": {
    "dev": "docker-compose up -d && cd frontend && npm start",
    "dev:install": "docker-compose up -d && cd frontend && npm install && npm start",
    "backend:start": "docker-compose up -d",
    "backend:stop": "docker-compose down",
    "backend:logs": "docker-compose logs -f app",
    "backend:migrate": "docker-compose exec app php artisan migrate",
    "frontend:install": "cd frontend && npm install",
    "frontend:start": "cd frontend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "vercel-build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "deploy:frontend": "cd frontend && vercel --prod"
  },
  "keywords": [
    "hydrosense",
    "water",
    "management",
    "react",
    "laravel"
  ],
  "author": "",
  "license": "MIT"
}
